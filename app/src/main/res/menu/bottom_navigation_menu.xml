<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Item pour la page d'accueil -->
    <item
        android:id="@+id/navigation_home"
        android:icon="@drawable/ic_home"
        android:title="Accueil" />

    <!-- Item pour la page d'enregistrement -->
    <item
        android:id="@+id/navigation_record"
        android:icon="@drawable/ic_directions_bike"
        android:title="Enregistrement" />

    <!-- Item pour la page historique -->
    <item
        android:id="@+id/navigation_history"
        android:icon="@drawable/ic_history"
        android:title="Historique" />

    <!-- Item pour la page des capteurs -->
    <item
        android:id="@+id/navigation_sensor"
        android:icon="@drawable/ic_sensor"
        android:title="Capteurs" />
</menu>
